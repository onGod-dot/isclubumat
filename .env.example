# Google Drive API key (server-side only — never exposed to the browser)
# Get one at: https://console.cloud.google.com → APIs & Services → Credentials
# Enable "Google Drive API" for the project, then create an API key
GOOGLE_DRIVE_API_KEY=
